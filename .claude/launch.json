{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "frontend",
      "runtimeExecutable": "bash",
      "runtimeArgs": ["-c", "npm --prefix frontend install --silent && npm --prefix frontend run dev"],
      "port": 3000
    },
    {
      "name": "backend",
      "runtimeExecutable": "bash",
      "runtimeArgs": ["-c", "cd backend && pip install -r requirements.txt -q && uvicorn main:app --reload --host 0.0.0.0 --port 8000"],
      "port": 8000
    }
  ]
}
